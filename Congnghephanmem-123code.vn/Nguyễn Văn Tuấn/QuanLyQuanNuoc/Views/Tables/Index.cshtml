@model IEnumerable<QuanLyQuanNuoc.Models.Table>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Thông tin</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.idTable }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.idTable }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.idTable })
        </td>
    </tr>
}

</table>
